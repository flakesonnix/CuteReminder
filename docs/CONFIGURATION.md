# Configuration — CuteReminder

Same as template:

```yaml
database:
  type: sqlite
  sqlite: { file: database.db }
ping:
  save-history: true
```

No extra config for `/joke` (uses public API). Future reminder config will live here.
